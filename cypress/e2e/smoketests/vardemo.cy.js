describe('template spec', () => {

    var username = "Admin"

    it('passes', () => {
      cy.visit('https://example.cypress.io')

      var ordernumber = "4987501893458904"
    })

    it('passes2', () => {

        let username = "Raju"
        cy.visit('https://example.cypress.io')

        cy.log(Username)
      })

      it('passes3', () => {
        cy.visit('https://example.cypress.io')
      })
    
  })